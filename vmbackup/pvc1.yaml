apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: boot-pvc
  namespace: mcdonaldstest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
