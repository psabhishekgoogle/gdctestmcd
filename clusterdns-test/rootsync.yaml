apiVersion: configsync.gke.io/v1beta1
kind: RootSync
metadata:
  annotations:
    client.lifecycle.config.k8s.io/deletion: detach
    configsync.gke.io/cluster-name-selector: db-test    
  name: platform
  namespace: config-management-system
spec:
  git:
    auth: token
    branch: testroot2
    dir: /clusterdns-test
    period: 15s
    repo: https://github.com/psabhishekgoogle/gdctestmcd    
    secretRef:
      name: github-token
  sourceFormat: unstructured
  sourceType: git
---
apiVersion: configsync.gke.io/v1beta1
kind: RootSync
metadata:
  annotations:
    client.lifecycle.config.k8s.io/deletion: detach    
    configsync.gke.io/cluster-name-selector: db-test    
  name: workload
  namespace: config-management-system
spec:
  git:
    auth: token
    branch: testroot3
    dir: /clusterdns-test
    period: 15s
    repo: https://github.com/psabhishekgoogle/gdctestmcd    
    secretRef:
      name: github-token
  sourceFormat: unstructured
  sourceType: git
